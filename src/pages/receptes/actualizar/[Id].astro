---
import ReceptesController from '../../../classes/ReceptesController';
import Layout from "../../../layouts/Layout.astro"
import Back from "../../../components/Back.astro";
import { Debug } from 'astro:components';



const { Id } = Astro.props;
// let nomb;
const receptesController = new ReceptesController();
// console.log(receptesController.getReceptaById(Id))


if(Astro.request.method === 'POST'){
    const data = await Astro.request.formData();
    const nom = data.get("nom");
    // const img = data.get("foto");
    // const descripcio = data.get("descripcio");
    const id = Id
    // console.log(id + " " + nom + " " + img + " " + descripcio);
    await receptesController.updateRecepta(id, nom);
    return Astro.redirect("/");
}


---

<Layout title="Nova recepta">

    <Back />

    <form class="relative" method="post">
        <div class="md:flex md:items-center mb-6">
            <div class="md:w-1/3">
                <label
                    class="block text-gray-500 md:text-right mb-1 md:mb-0 pr-4"
                    for="inline-full-name"
                >
                    Nom
                </label>
            </div>
            <div class="md:w-2/3">
                <input
                    class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700"
                    name="nom"
                    type="text"
                />
            </div>
        </div>
        <div class="md:flex md:items-center mb-6">
            <div class="md:w-1/3">
                <label
                    class="block text-gray-500 md:text-right mb-1 md:mb-0 pr-4"
                    for="inline-full-name"
                >
                    Foto
                </label>
            </div>
            <div class="md:w-2/3">
                <input
                    class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700"
                    name="foto"
                    type="text"
                />
            </div>
        </div>
        <div class="md:flex md:items-center mb-6">
            <div class="md:w-1/3">
                <label
                    class="block text-gray-500 md:text-right mb-1 md:mb-0 pr-4"
                    for="inline-full-name"
                >
                    Descripcio
                </label>
            </div>
            <div class="md:w-2/3">
                <textarea
                    class="bg-gray-200 appearance-none border-2 border-gray-200 rounded w-full py-2 px-4 text-gray-700"
                    name="descripcio"
                />
            </div>
        </div>
        <!-- <input name="id" type="hidden" value={Id} > -->
        <button class="text-white bg-[#55cf5d] duration-300 hover:transition-all hover:bg-[#46332f] rounded-full block   mb-9  p-3 right-0">Enviar</button>
    </form>
    <Debug answer={Id} />
</Layout>
